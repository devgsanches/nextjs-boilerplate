import { Meta, StoryObj } from '@storybook/react'
import { Header } from '.'

export default {
  title: 'app/(app)/_components/Header',
  component: Header,
  tags: ['autodocs'],
  parameters: {
    layout: 'fullscreen'
  }
} as Meta
export const Default: StoryObj = {}